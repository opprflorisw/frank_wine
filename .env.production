# Production Convex deployment (public client URL — safe to commit).
# The frontend connects to this; the Gemini key lives only on the Convex backend.
VITE_CONVEX_URL=https://impartial-axolotl-179.eu-west-1.convex.cloud
